namespace Processor
{
    public enum Format
    {
        Lang,
        Json
    }

    public enum Language
    {
        English,
        Chinese
    }

    public enum IsNeeded
    {
        Requested,
        Unneeded
    }
}