window {
    size(168,20)

    bind('public','public')

    panel('','vertical') {
        bgthickness(2)
        togglebutton('public','公开') {
            channel('public')
            desired(160,16)
            tooltips('选中此选项后','频道将会被公开')
            check(true)
        }
    }
}
