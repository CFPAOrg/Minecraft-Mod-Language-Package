the following functions are defined on the Interop Module
* `popFloat() -> Number`
  reads the oldest number as a decimal
* `popInt() -> Number`
  reads the oldest number as a Signed Int
* `pushNumber(Number)`
  pushes a number (signed,float is determined automatically)