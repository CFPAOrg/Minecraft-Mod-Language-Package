{
  "name": "Detector Module",
  "category": "modules",
  "icon": "modularrouters:detector_module",
  "pages": [
    {
      "type": "text",
      "text": "This module doesn't actually manipulate items, but instead detects specific items in the router's buffer. If the buffer contents are matched by the module's filter, it will make the router emit a redstone signal in the $(l:intro/modules#direction)configured direction/$.$(p)The signal level (default: 15) and signal type (default: weak) can be adjusted via the module GUI."
    },
    {
      "type": "crafting",
      "recipe": "modularrouters:detector_module"
    }
  ]
}
