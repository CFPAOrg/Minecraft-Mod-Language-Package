window {
    size(168,20)

    bind('public','public')

    panel('','vertical') {
        bgthickness(2)
        togglebutton('public','Make public') {
            channel('public')
            desired(160,16)
            tooltips('If selected this channel','will be publicly available')
            check(true)
        }
    }
}
